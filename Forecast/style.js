import React, {
   StyleSheet
 } from 'react-native';

var styles = StyleSheet.create({
  weatherInfo : {
    alignItems: 'center'
  },
  bigText: {
    flex: 2,
    fontSize: 30
  },
  mainText: {
    flex: 1,
    fontSize: 16
  }
});

export default styles;
