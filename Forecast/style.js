import React, {
   StyleSheet
 } from 'react-native';

var styles = StyleSheet.create({
  weatherInfo : {
    alignItems: 'center'
  },
  bigText: {
    flex: 2,
    fontSize: 30,
    color: '#FFFFFF'
  },
  mainText: {
    flex: 1,
    fontSize: 16,
    color: '#FFFFFF'
  }
});

export default styles;
